spring:
  datasource:
    #TODO add SSL connection
    url: jdbc:h2:mem:dos
    username: dos
    password: dos
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate.dialect: org.hibernate.dialect.H2Dialect
  h2:
    console:
      enabled: true